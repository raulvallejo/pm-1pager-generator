# Copy to .env.local — Vite exposes only vars prefixed with VITE_ to the browser
# In production (Vercel), set this in the Vercel dashboard under Environment Variables

# The deployed Railway backend URL (no trailing slash)
# Leave empty to use the Vite dev proxy (recommended for local development)
VITE_API_BASE_URL=https://your-app.up.railway.app
